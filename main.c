#include <stdio.h>

int main() {
    // The maximum value obtained from the warm-up activity is $650
    int max_value = 650;

    // Print the message with the maximum value
    printf("The maximum value is %d\n", max_value);

    return 0;
}
